// tailwind config is required for editor support

// const sharedConfig = require("tailwind-config/tailwind.config.js");
const sharedConfig = require("@onivue/ui/tailwind.config.js");

module.exports = {
  presets: [sharedConfig],
};
